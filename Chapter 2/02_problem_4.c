#include <stdio.h>

int main() {
      printf("Hello World!!!\n");
    // Your code here
    int   a = 1;
    float x = 3.0;
      printf("The value of a is %d and the value of x is %f\n", a, x);
    float res = x + a;
      printf("The result of x+a is %f\n", res);

    return 0;
}